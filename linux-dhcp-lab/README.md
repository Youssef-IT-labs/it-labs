# Linux DHCP Lab

## Objective

The goal of this lab is to configure a DHCP server on Ubuntu to automatically assign IP addresses to clients.

## Environment

* Ubuntu Server
* ISC DHCP Server
* Local network

## Goal

Automatically assign IP addresses to devices in a custom range.
